<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Base app theme. AppCompatActivity (used for locale handling) needs
        an AppCompat parent; Compose draws everything on top anyway.
    -->
    <style name="Theme.Upscaler" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Splash screen (androidx.core:core-splashscreen compat API). -->
    <style name="Theme.Upscaler.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenAnimationDuration">400</item>
        <item name="postSplashScreenTheme">@style/Theme.Upscaler</item>
    </style>
</resources>
